import { CreditEarnedPipe } from './credit-earned.pipe';

describe('CreditEarnedPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditEarnedPipe();
    expect(pipe).toBeTruthy();
  });
});
