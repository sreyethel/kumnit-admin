import { DaysSchedulePipe } from './days-schedule.pipe';

describe('DaysSchedulePipe', () => {
  it('create an instance', () => {
    const pipe = new DaysSchedulePipe();
    expect(pipe).toBeTruthy();
  });
});
