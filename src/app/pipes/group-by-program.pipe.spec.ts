import { GroupByProgramPipe } from './group-by-program.pipe';

describe('GroupByProgramPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByProgramPipe();
    expect(pipe).toBeTruthy();
  });
});
