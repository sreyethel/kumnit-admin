import { SelectedItemByKeyPipe } from './selected-item-by-key.pipe';

describe('SelectedItemByKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedItemByKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
