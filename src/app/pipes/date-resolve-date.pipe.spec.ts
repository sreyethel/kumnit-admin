import { DateResolveDatePipe } from './date-resolve-date.pipe';

describe('DateResolveDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DateResolveDatePipe();
    expect(pipe).toBeTruthy();
  });
});
