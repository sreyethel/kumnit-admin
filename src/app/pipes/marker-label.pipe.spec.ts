import { MarkerLabelPipe } from './marker-label.pipe';

describe('MarkerLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkerLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
