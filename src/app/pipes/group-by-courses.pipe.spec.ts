import { GroupByCoursesPipe } from './group-by-courses.pipe';

describe('GroupByCoursesPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByCoursesPipe();
    expect(pipe).toBeTruthy();
  });
});
