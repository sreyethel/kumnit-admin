import { FilterSchedulePipe } from './filter-schedule.pipe';

describe('FilterSchedulePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSchedulePipe();
    expect(pipe).toBeTruthy();
  });
});
