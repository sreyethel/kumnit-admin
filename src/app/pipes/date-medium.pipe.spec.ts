import { DateMediumPipe } from './date-medium.pipe';

describe('DateMediumPipe', () => {
  it('create an instance', () => {
    const pipe = new DateMediumPipe();
    expect(pipe).toBeTruthy();
  });
});
