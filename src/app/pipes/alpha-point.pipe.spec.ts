import { AlphaPointPipe } from './alpha-point.pipe';

describe('AlphaPointPipe', () => {
  it('create an instance', () => {
    const pipe = new AlphaPointPipe();
    expect(pipe).toBeTruthy();
  });
});
