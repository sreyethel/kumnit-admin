import { AlphaGradePipe } from './alpha-grade.pipe';

describe('AlphaGradePipe', () => {
  it('create an instance', () => {
    const pipe = new AlphaGradePipe();
    expect(pipe).toBeTruthy();
  });
});
